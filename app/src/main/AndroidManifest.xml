<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wamod">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:debuggable="true"
        android:name="wamod.App">


        <activity
            android:name="wamod.activity.HomeActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:theme="@style/Theme.WAMOD.Home"
            android:hardwareAccelerated="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.whatsapp.SettingsAccount" />
        <activity android:name="com.whatsapp.SettingsChats" />
        <activity android:name="com.whatsapp.SettingsNotifications" />
        <activity android:name="com.whatsapp.SettingsDataUsage" />
        <activity android:name="com.whatsapp.SettingsContacts" />
        <activity android:name="com.whatsapp.SettingsHelp" />

    </application>

</manifest>
